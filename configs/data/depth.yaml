kitti:
  type: video  # with sequences of video
  root_path: data/kitti//depth_selection/val_selection_cropped
  ls_all_seqs: ${data.kitti.root_path}/image_gathered
  img:
    path: ${data.kitti.root_path}/image_gathered/{seq}
    ext: png
  depth:
    path: ${data.kitti.root_path}/groundtruth_depth_gathered/{seq}
    ext: png

bonn:
  type: video
  root_path: data/bonn/rgbd_bonn_dataset
  ls_all_seqs:
    - rgbd_bonn_balloon2
    - rgbd_bonn_crowd2
    - rgbd_bonn_crowd3
    - rgbd_bonn_person_tracking2
    - rgbd_bonn_synchronous
  img:
    path: ${data.bonn.root_path}/{seq}/rgb_110
    ext: png
  depth:
    path: ${data.bonn.root_path}/{seq}/depth_110
    ext: png

sintel:
  type: video
  root_path: data/sintel/training
  ls_all_seqs:
    - alley_2
    - ambush_4
    - ambush_5
    - ambush_6
    - cave_2
    - cave_4
    - market_2
    - market_5
    - market_6
    - shaman_3
    - sleeping_1
    - sleeping_2
    - temple_2
    - temple_3
  img:
    path: ${data.sintel.root_path}/final/{seq}
    ext: png
  depth:
    path: ${data.sintel.root_path}/depth/{seq}
    ext: dpt
  # anno:
  #   path: ${data.sintel.root_path}/camdata_left

nyu-v2:
  type: mono  # only a set of images, without sequences
  root_path: data/nyu-v2/val
  img:
    path: ${data.nyu-v2.root_path}/nyu_images
    ext: png
  depth:
    path: ${data.nyu-v2.root_path}/nyu_depths
    ext: npy


flsea-canyons:
  type: video
  root_path: /data/users/yihao/datasets/flsea_100/canyons
  ls_all_seqs: ${data.flsea-canyons.root_path}
  img:
    path: ${data.flsea-canyons.root_path}/{seq}/imgs
    ext: tiff
  depth:
    path: ${data.flsea-canyons.root_path}/{seq}/depth
    ext: tif

flsea-redsea:
  type: video
  root_path: /data/users/jiayi/datasets/flsea_100/red_sea
  ls_all_seqs: ${data.flsea-redsea.root_path}
  img:
    path: ${data.flsea-redsea.root_path}/{seq}/imgs
    ext: tiff
  depth:
    path: ${data.flsea-redsea.root_path}/{seq}/depth
    ext: tif

seathru:
  type: video
  root_path: /fs/jiayi/seathru
  ls_all_seqs: 
    - D1
    - D2
    - D3
    - D4
    - D5
  img:
    path: ${data.seathru.root_path}/{seq}/{seq}/linearPNG
    ext: png
  depth:
    path: ${data.seathru.root_path}/{seq}/{seq}/depth
    ext: tif

squid:
  type: squid  # special type for SQUID stereo dataset
  root_path: /fs/jiayi/SQUID
  ls_all_seqs:
    - Katzaa
    - Michmoret
    - Nachsolim
    - Satil


usod10k:
  type: mono
  root_path: /fs/jiayi/USOD10k
  img:
    path: ${data.usod10k.root_path}/TE/RGB
    ext: png
  depth:
    path: ${data.usod10k.root_path}/TE/depth
    ext: png

uieb:
  type: mono
  root_path: /fs/jiayi/UIEB
  img:
    path: ${data.uieb.root_path}
    ext: png